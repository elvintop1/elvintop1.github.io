{
  "name": "quantum-content-admin-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute quantum-content-admin --local --file=schema.sql",
    "db:remote": "wrangler d1 execute quantum-content-admin --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.119.0"
  }
}
